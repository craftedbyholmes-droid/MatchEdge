MATCHEDGE BUILD NOTES

This fixed version resolves the relative-path write bug.

Run it with:
Set-ExecutionPolicy -Scope Process Bypass
.\build_matchedge_project.ps1 -ProjectRoot "C:\Projects\matchedge"

If you still see the wrong folder name in errors, you are running an older script file.